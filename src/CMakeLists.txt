add_subdirectory(client)
if(GreenIsland_FOUND AND Qt5Compositor_FOUND)
    add_subdirectory(compositor)
endif()
add_subdirectory(declarative)
add_subdirectory(platformthemes)
add_subdirectory(server)
